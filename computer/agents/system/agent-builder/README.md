# Agent Builder

Builds executable agent apps.

This agent must create more than prompt files. When a requested agent needs capabilities, Agent Builder should add tools, templates, tests, examples, and registration updates.
