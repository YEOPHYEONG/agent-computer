# Memory

Workspace memory lives here.

Public repositories should include only examples and placeholders.

Use files like:

- `user-preferences.example.md`
- `workspace-context.example.md`
- `reusable-patterns.example.md`

Do not commit private memory.
