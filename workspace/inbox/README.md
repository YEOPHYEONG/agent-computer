# Inbox

Put source files here when you want Agent Computer to process them.

Examples:

- PDFs to convert with `document-ingestor`
- notes to turn into reports
- source material for a deck
- CSV exports for analysis agents

For durable outputs, Agent Computer should create a project folder under `projects/<project-slug>/`.

Do not commit private inbox files.
