# Outputs

This is a temporary staging area.

Durable user-facing outputs should normally live under:

```text
projects/<project-slug>/
```

If a helper tool writes temporary previews or intermediate files here, the final useful artifacts should be copied or moved into the active project and documented in QA.

Do not commit generated output artifacts.
